#include "pch.h"
/*
template <typename T>
_com_ptr_t<T>::_com_ptr_t() {
    this->m_pInterface = 0;
}

template <typename T>
_com_ptr_t<T>::~_com_ptr_t() {
    if (this->m_pInterface)
        delete this->m_pInterface;
}

_com_ptr_t<IUnknown>::~_com_ptr_t() {
    if (this->m_pInterface)
        this->m_pInterface->Release();
}


template <typename T>
T* _com_ptr_t<T>::operator*() {
    return this->m_pInterface;
}

template <typename T>
T* _com_ptr_t<T>::operator->() {
    return this->m_pInterface;
}

*/
